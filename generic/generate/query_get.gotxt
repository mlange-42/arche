// Get{{ .ID }} returns the first queried component for the current query position
func (q *Query{{ .Query.Index }}{{ .Query.Types }}) Get{{ .ID }}() *{{ .Type }} {
	return (*{{ .Type }})(q.Query.Get(q.ids[{{ .Index }}]))
}
